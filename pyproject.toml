[project]
name = "ai-sdk-python"
version = "0.0.0"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["packages/*"]

[tool.coverage.run]
source = [
    "packages/ai-api-client-sdk/ai_api_client_sdk",
    "packages/ai-core-sdk/ai_core_sdk",
    "packages/generative-ai-hub-sdk/gen_ai_hub",
]
